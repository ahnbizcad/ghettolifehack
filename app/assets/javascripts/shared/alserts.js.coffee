# Make alerts disappear after some time. 
# Bad because this unexpectedly shifts the entire page.
# One solution is to keep everything else in position, leaving empty space where the alert was.

#$(document).ready ->
#  setTimeout (->
#    $(".alert-dismissable").fadeOut("slow")
#  ), 5000

